# Java Maven Project

## Overview
Minimal Java project built with Maven. Includes build, run, and test instructions.

## Requirements
\- `JDK 17+`
\- `Maven 3.6+`

Verify:
```bash
java -version
mvn -v